recursive: true

packages:
  github.com/devantler-tech/ksail-go:
    config:
      all: true
  github.com/docker/docker/client:
    config:
      recursive: false
      dir: pkg/provisioner/cluster/kind
      filename: mocks_containerapiclient_test.go
      pkgname: kindprovisioner
    interfaces:
      ContainerAPIClient:
