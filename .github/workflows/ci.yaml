name: CI - Go (Repo)

on:
  push:
    branches: [main]
  pull_request:

permissions: {}

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: 📄 Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false
          fetch-depth: 0

      - name: ⚙️ Setup Go
        uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00 # v6.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: 📦 Download dependencies
        run: go mod download

      - name: 🔧 Install mockery
        run: go install github.com/vektra/mockery/v3@latest

      - name: 🏃 Run pre-commit hooks
        uses: pre-commit/action@2c7b3805fd2a0fd8c1884dcaebf91fc102a13ecd # v3.0.1

  ci:
    needs: [pre-commit]
    if: github.event_name == 'push'
    uses: devantler-tech/reusable-workflows/.github/workflows/ci-go.yaml@800bac1f7de14fcc6b9b9bcf62e201308eafd15b # v1.15.12
    secrets:
      CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    permissions:
      contents: write
      issues: write
      packages: read
      pull-requests: write

  system-test:
    runs-on: ubuntu-latest
    # Run system tests on both push and pull request, but only after pre-commit and ci jobs complete
    needs: [pre-commit, ci]
    if: always() && (needs.ci.result == 'success' || needs.ci.result == 'skipped')
    strategy:
      matrix:
        include:
          - init-args: "--distribution Kind"
          - init-args: "--distribution Kind --cni Cilium"
          - init-args: "--distribution Kind --mirror-registry docker.io=https://registry-1.docker.io"
            test-workload: true
          - init-args: "--distribution Kind --mirror-registry docker.io=https://registry-1.docker.io --mirror-registry ghcr.io=https://ghcr.io"
            test-workload: true
          - init-args: "--distribution K3d"
          - init-args: "--distribution K3d --cni Cilium"
          - init-args: "--distribution K3d --mirror-registry docker.io=https://registry-1.docker.io"
            test-workload: true
          - init-args: "--distribution K3d --mirror-registry docker.io=https://registry-1.docker.io --mirror-registry ghcr.io=https://ghcr.io"
            test-workload: true
          - init-args: "--distribution K3d --metrics-server Disabled"
    steps:
      - name: 📄 Checkout
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0
        with:
          persist-credentials: false

      - name: ⚙️ Setup Go
        uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00 # v6.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: 📦 Download dependencies
        run: go mod download

      - name: 🏗️ Build application
        run: go build -o ksail .

      - name: 🧪 ksail cluster init
        run: |
          ./ksail cluster init ${{ matrix.init-args }}

      - name: 🧪 ksail cluster create
        run: |
          ./ksail cluster create

      - name: 🧪 ksail cluster list
        run: |
          ./ksail cluster list

      - name: 🧪 ksail workload create
        if: matrix.test-workload
        run: |
          ./ksail workload create deployment whoami --image=traefik/whoami:latest
          ./ksail workload wait --for=condition=Available deployment/whoami --timeout=120s

      - name: 🧪 ksail workload apply
        if: matrix.test-workload
        run: |
          # Use ghcr.io image if ghcr.io mirror is configured, otherwise use docker.io
          if echo "${{ matrix.init-args }}" | grep -q "ghcr.io"; then
            IMAGE="ghcr.io/nginxinc/nginx-unprivileged:alpine"
            PORT="8080"
          else
            IMAGE="nginx:alpine"
            PORT="80"
          fi
          # Create a simple deployment manifest
          cat > /tmp/test-deployment.yaml <<EOF
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: test-app
            labels:
              app: test-app
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: test-app
            template:
              metadata:
                labels:
                  app: test-app
              spec:
                containers:
                - name: test-app
                  image: ${IMAGE}
                  ports:
                  - containerPort: ${PORT}
          EOF
          # Apply deployment using ksail
          ./ksail workload apply -f /tmp/test-deployment.yaml
          # Wait for deployment to become available
          ./ksail workload wait --for=condition=Available deployment/test-app --timeout=300s

      - name: 🧪 ksail workload reconcile
        run: |
          ./ksail workload reconcile

      - name: 🧪 ksail cluster stop
        run: |
          ./ksail cluster stop

      - name: 🧪 ksail cluster start
        run: |
          ./ksail cluster start

      - name: 🧪 ksail cluster delete
        run: |
          ./ksail cluster delete

      - name: 🧹 Cleanup
        run: |
          if [ -d "k8s" ]; then rm -rf "k8s"; fi
          if [ -f "kind.yaml" ]; then rm "kind.yaml"; fi
          if [ -f "k3d.yaml" ]; then rm "k3d.yaml"; fi
          if [ -f "ksail.yaml" ]; then rm "ksail.yaml"; fi

  system-test-status:
    runs-on: ubuntu-latest
    needs: [system-test]
    if: always()
    steps:
      - name: Summarize matrix result
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "system-test result: $MATRIX_RESULT"
          case "$MATRIX_RESULT" in
            success|skipped)
              echo "All matrix runs succeeded or were skipped."
              exit 0
              ;;
            *)
              echo "At least one matrix run failed."
              exit 1
              ;;
          esac
        env:
          MATRIX_RESULT: ${{ needs.system-test.result }}
